{#
    Alert title — the heading line of an alert.

    Usage:
      <twig:Tabler:Alert:Title>Saved!</twig:Tabler:Alert:Title>
#}
<h4 {{ attributes.defaults({class: 'alert-title'}) }}>{%- block content %}{% endblock -%}</h4>
