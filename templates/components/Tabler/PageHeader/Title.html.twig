{#
    Page header title — the main page heading.

    Usage:
        <twig:Tabler:PageHeader:Title>Dashboard</twig:Tabler:PageHeader:Title>
#}
<h2 {{ attributes.defaults({class: 'page-title'}) }}>{%- block content %}{% endblock -%}</h2>
