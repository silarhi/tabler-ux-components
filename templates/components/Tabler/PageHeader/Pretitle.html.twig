{#
    Page header pretitle — the small overline above the title.

    Usage:
        <twig:Tabler:PageHeader:Pretitle>Overview</twig:Tabler:PageHeader:Pretitle>
#}
<div {{ attributes.defaults({class: 'page-pretitle'}) }}>{%- block content %}{% endblock -%}</div>
