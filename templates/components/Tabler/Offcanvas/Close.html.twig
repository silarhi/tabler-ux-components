{#
    Offcanvas close button — dismisses the panel via Bootstrap's data API.

    Usage:
        <twig:Tabler:Offcanvas:Close />
#}
<button type="button" {{ attributes.defaults({class: 'btn-close', 'data-bs-dismiss': 'offcanvas', 'aria-label': 'Close'}) }}></button>
