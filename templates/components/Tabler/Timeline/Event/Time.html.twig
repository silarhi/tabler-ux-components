{#
    Timeline event time — the timestamp shown on the right of a timeline event.

    Usage:
        <twig:Tabler:Timeline:Event:Time>1 day ago</twig:Tabler:Timeline:Event:Time>
#}
<div {{ attributes.defaults({class: 'text-secondary float-end'}) }}>{%- block content %}{% endblock -%}</div>
