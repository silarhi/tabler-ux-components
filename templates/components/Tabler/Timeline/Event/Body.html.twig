{#
    Timeline event body — the descriptive text of a timeline event.

    Usage:
        <twig:Tabler:Timeline:Event:Body>Download the <a href="#">latest backup</a>.</twig:Tabler:Timeline:Event:Body>
#}
<div {{ attributes.defaults({class: 'text-secondary'}) }}>{%- block content %}{% endblock -%}</div>
