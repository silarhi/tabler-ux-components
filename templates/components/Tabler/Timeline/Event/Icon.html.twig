{#
    Timeline event icon — the dot/icon marker of a timeline event (slot-only).

    Usage:
        <twig:Tabler:Timeline:Event:Icon><twig:Tabler:Icon name="check" /></twig:Tabler:Timeline:Event:Icon>
#}
<div {{ attributes.defaults({class: 'timeline-event-icon'}) }}>{%- block content %}{% endblock -%}</div>
