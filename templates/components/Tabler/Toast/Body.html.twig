{#
    Toast body — the message area of a toast.

    Usage:
        <twig:Tabler:Toast:Body>Saved successfully.</twig:Tabler:Toast:Body>
#}
<div {{ attributes.defaults({class: 'toast-body'}) }}>{%- block content %}{% endblock -%}</div>
