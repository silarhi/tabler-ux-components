{#
    Card body — the main content area of a card.

    Usage:
      <twig:Tabler:Card:Body>Some content.</twig:Tabler:Card:Body>
#}
<div {{ attributes.defaults({class: 'card-body'}) }}>{%- block content %}{% endblock -%}</div>
