{#
    Dropdown header — a non-interactive label above a group of items.

    Usage:
        <twig:Tabler:Dropdown:Header>Actions</twig:Tabler:Dropdown:Header>
#}
<div {{ attributes.defaults({class: 'dropdown-header'}) }}>{%- block content %}{% endblock -%}</div>
