{#
    Switch icon state A — the first (default) icon of a <twig:Tabler:SwitchIcon>.

    Usage:
        <twig:Tabler:SwitchIcon:A><twig:Tabler:Icon name="moon" /></twig:Tabler:SwitchIcon:A>
#}
<span {{ attributes.defaults({class: 'switch-icon-a'}) }}>{%- block content %}{% endblock -%}</span>
