{#
    Empty icon — the leading visual of an empty state (slot-only).

    Usage:
      <twig:Tabler:Empty:Icon><twig:Tabler:Icon name="mood-empty" /></twig:Tabler:Empty:Icon>
#}
<div {{ attributes.defaults({class: 'empty-icon'}) }}>{%- block content %}{% endblock -%}</div>
