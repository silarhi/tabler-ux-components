{#
    Modal body — the main content area of a modal.

    Usage:
      <twig:Tabler:Modal:Body>Some content.</twig:Tabler:Modal:Body>
#}
<div {{ attributes.defaults({class: 'modal-body'}) }}>{%- block content %}{% endblock -%}</div>
