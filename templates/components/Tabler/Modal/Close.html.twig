{#
    Modal close button — dismisses the modal via Bootstrap's data API.

    Usage:
      <twig:Tabler:Modal:Close />
#}
<button type="button" {{ attributes.defaults({class: 'btn-close', 'data-bs-dismiss': 'modal', 'aria-label': 'Close'}) }}></button>
